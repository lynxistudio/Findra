# Findra 迭代实施计划 (Roadmap)

本文档制定 Findra 从极速索引搜索工具升级整合为轻量、高性能 macOS 原生双模文件管理器的完整路线图。

---

## 目标愿景
- **轻量原生**：纯 Swift / AppKit / SwiftUI 打造，零外部依赖，极低内存与 CPU 占用。
- **双模体验**：
  1. **极速搜索模式**：保留现有的毫秒级 SQLite/FTS5 检索、NAS（SMB/NFS）自动定时同步与原子重构能力。
  2. **层级浏览模式**：完整的目录树浏览、多级深入、前进/后退/上级跳跃及交互式面包屑导航。
- **解决访达痛点**：
  1. **流式异步缩略图**：在 NAS 和大型媒体文件夹中，多级缓存 + 流式并发防拥堵 + 视口外任务取消，彻底告别访达卡死与掉帧。
  2. **符合直觉的操作**：原生支持 `Cmd+X` 剪切（配合虚化视觉）、`Cmd+C` 复制、`Cmd+V` 智能粘贴（自动副本命名）、`Return` 快速重命名、`Cmd+Delete` 安全移入废纸篓。
  3. **一键强制刷新**：`F5` / `Cmd+R` 实时重载目录与同步索引。
  4. **全键盘流 Quick Look**：空格键呼出预览，方向键切换选中项时光标与预览实时同步。

---

## 阶段演进规划

### 阶段 1：导航引擎与目录浏览核心 (Browse Engine & Navigation)
- [ ] **数据库架构增强**：
  - 在 `files` 表中维护 `parent_path` 字段，建立 `idx_files_parent` 索引，实现父目录下直接子项的 B-Tree 快速读取（< 2ms）。
  - 实现双模数据源：已索引目录从 SQLite 快速读取，未索引目录直接调用 `FileManager.contentsOfDirectory` 实时遍历。
- [ ] **导航状态控制器**：
  - `AppState` 引入当前目录 `currentDirectoryPath`。
  - 完善导航历史栈（`backStack` / `forwardStack`），提供 `navigateBack()`、`navigateForward()`、`navigateUp()`。
- [ ] **导航与工具栏组件**：
  - 顶部导航栏：前进/后退按钮、可点击的面包屑路径组件（Path Bar）、刷新按钮、视图模式切换按钮（列表 / 网格）。
  - 侧边栏与主区联动：点击侧边栏目录直接进入该目录浏览；在搜索框有文字时展示搜索结果，清空时自动恢复目录浏览。
- [ ] **双击与快捷键**：
  - 双击文件夹进入下一级目录。
  - 支持 `Cmd+[` 后退、`Cmd+]` 前进、`Cmd+Up` 返回上一级。

### 阶段 2：日常文件操作与剪切板体系 (File Operations & Clipboard)
- [ ] **智能剪贴板（Cmd+X / Cmd+C / Cmd+V）**：
  - `Cmd+X` 剪切：设置剪切标记，将选中的文件 URL 写入系统剪切板，同时在 UI 中将对应卡片/行虚化（opacity 0.5）。
  - `Cmd+C` 复制：标准文件 URL 写入剪切板，清除剪切视觉状态。
  - `Cmd+V` 粘贴：如果是剪切操作，执行移动并同步数据库更新；如果是复制操作，执行文件拷贝并自动处理同名冲突（自动命名为 `xxx 副本.ext` 或 `xxx 2.ext`）。
- [ ] **快速重命名 (Return / Enter)**：
  - 选中单个文件或文件夹时按回车键，进入就地编辑框（支持列表与网格）；Esc 取消，Enter 提交。
- [ ] **安全删除 (Cmd+Delete / 废纸篓)**：
  - 支持快捷键与上下文菜单移入废纸篓，并自动从当前视图和 SQLite 中清理。
- [ ] **一键强制刷新 (F5 / Cmd+R)**：
  - 实时重新扫描当前目录，刷新缓存并重载 UI。

### 阶段 3：流式异步缩略图与高性能网格视图 (Streaming Thumbnail Grid)
- [ ] **缩略图核心架构 (`ThumbnailManager`)**：
  - 内存缓存：`NSCache<NSString, NSImage>` 高速复用。
  - 磁盘缓存：在 `~/Library/Application Support/Findra/Thumbnails/` 下按 `sha256(path + modDate)` 持久化缩略图，二次加载 0 开销。
  - 任务调度器：并发控制在 4-6 个线程，避免抢占 NAS SMB/NFS 连接与主线程渲染。
  - 原生硬件加速：接入 `QLThumbnailGenerator`，高保真生成图片（JPG/PNG/HEIC/WebP/RAW）及视频关键帧缩略图。
  - 视口动态加载：进入视口加载，滑出视口及时取消请求；加载期间使用系统文件图标平滑过渡。
- [ ] **网格视图 (`FileGridView`)**：
  - 弹性网格布局，支持缩略图卡片缩放调节（小 64px、中 128px、大 256px）。
  - 支持多选（Cmd/Shift 点击）、拖拽与右键上下文菜单。

### 阶段 4：空格预览 (Quick Look) 同步与键盘交互闭环
- [ ] **联动 Quick Look**：
  - 空格键开关预览面板。
  - 预览打开状态下，用户使用方向键移动选中项时，预览面板内容实时跟随切换。
- [ ] **全局快捷键与焦点优化**：
  - 统一搜索框、路径条、文件列表/网格之间的焦点切换逻辑。

### 阶段 5：网络卷韧性、性能调优与发布验证
- [ ] **NAS 稳定性优化**：
  - 优化高延迟网络驱动器（SMB/NFS）下的防假死与超时保护。
- [ ] **编译打包与回归测试**：
  - 运行 `./build.sh` 编译优化打包。
  - 更新项目记录：`HANDOFF.md`、`CHANGELOG.md`、`NEXT.md`。
